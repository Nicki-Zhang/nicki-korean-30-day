# Nikigo 开发待办

## K0 Audio Batch 1

- [x] `yo.mp3` / 요：SHA、媒体参数和 FFmpeg 完整解码技术验证通过。
- [x] `yu.mp3` / 유：SHA、媒体参数和 FFmpeg 完整解码技术验证通过。
- [ ] 产品负责人在课程设计和实际使用场景中试听 `yo.mp3`。
- [ ] 产品负责人在课程设计和实际使用场景中试听 `yu.mp3`。
- [ ] 仅在产品负责人逐条明确反馈“试听通过”后，记录 `reviewMethod: product-owner-listening`、`nativeReviewStatus: deferred`、`nativeReviewer: null`、`technicalValidation: passed` 和实际 `reviewedAt`。
- [ ] 在单独授权任务中接入通过试听的文件；未通过文件单独标记 `rejected`，不得影响同批次其他文件。
- [ ] 正式发布前按风险选择母语者抽检项目；该抽检不是当前课程开发和音频接入的强制门禁。

当前边界：两条音频均为 `underReview`；正式 manifest 仍为 `pending/missing`；播放器不启用。

