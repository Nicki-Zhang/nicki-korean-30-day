# Nikigo 开发待办

## K0 Audio Batch 1

- [x] `yo.mp3` / 요：SHA、媒体参数和 FFmpeg 完整解码技术验证通过。
- [x] `yu.mp3` / 유：SHA、媒体参数和 FFmpeg 完整解码技术验证通过。
- [x] 产品负责人在课程设计和实际使用场景中试听 `yo.mp3`：2026-07-20 通过。
- [x] 产品负责人在课程设计和实际使用场景中试听 `yu.mp3`：2026-07-20 通过。
- [x] 记录 `reviewMethod: product-owner-listening`、`nativeReviewStatus: deferred`、`nativeReviewer: null`、`technicalValidation: passed` 和实际 `reviewedAt`。
- [x] 按固定 SHA 接入两条通过试听的文件；未处理任何其他音频。
- [ ] 正式发布前按风险选择母语者抽检项目；该抽检不是当前课程开发和音频接入的强制门禁。

当前边界：两条音频均为 `approved/available`，只允许精确 speechText、audioType、SHA 和技术验证匹配时播放；`rightsReviewStatus` 仍为 `pending`，母语者抽检仍为可选发布前待办。

## 永久课程Sprint自查清单

以下清单适用于每个课程、音频、导航和发布 Sprint；不得因文件存在、步骤数正确、单元测试通过或页面可打开而跳过。

- [ ] 从最新正式基准创建独立功能分支；不触碰 `CODEX_HANDOFF.md`。
- [ ] 为每课输出源代码内容摘要，并验证目标、解释、实例、练习、答案、错题重练和真实知识点总结。
- [ ] 运行内容完整性、身份/进度键、答案隐私、音频精确匹配、Service Worker、敏感信息及完整 `npm test`。
- [ ] 运行 `git diff --check`，确认无非任务文件、封板音频或音频 catalog 变更。
- [ ] 创建全新临时 Chrome 用户目录，从课程首页点击进入，不直接打开内部测试 URL。
- [ ] 每课完成正确路线，并至少触发一次错误答案与错题重练。
- [ ] 验证课程返回、浏览器返回、刷新恢复、首次/重复完成和重新学习。
- [ ] 验证新用户、已有进度用户和已完成用户。
- [ ] 验证 `zh/en/vi/ja` 和 390×844、768×1024、1440×900。
- [ ] 检查控制台、网络4xx/5xx、最终URL、运行时lessonId、stableId、进度键、Service Worker和缓存版本。
- [ ] 保存每课开始教学页、中间练习页、总结页截图及机器可读结果。
- [ ] 关闭并删除临时 Chrome 上下文，确认真实用户学习数据未改变。
- [ ] 记录自查发现的所有问题、根因、修复与复验结果；阻断项不得降级。
- [ ] 报告分别标明“源代码存在 / 浏览器实际渲染 / 用户实际可操作”。
- [ ] 仅提交并推送功能分支；等待产品负责人确认后才合并或部署。

## 第0～13课内容完整性与路由审计（2026-07-21）

- [x] 定位第4课入口、canonical URL、运行时ID与第7课历史身份反转。
- [x] 第4课改为 `lesson-04` / `lesson-04.html`，第7课改为 `lesson-07` / `lesson-07.html`。
- [x] 增加幂等迁移，分别保留旧第4课 `k0-consonant-contrast` 与旧第7课 `lesson-04` 的进度、完成状态和会话。
- [x] 验证第4/7课“均未完成、均已完成、仅第7课完成、仅第4课完成”四种组合互不串课。
- [x] 第0～13课逐课检查开始、中央和完成前真实可见内容；未发现空白、错课、title-only或重复占位。
- [x] 确认第11～13课源内容各13步；“没有内容”由完成状态只显示完成页造成，不是源课程为空。
- [x] 为第4～13课完成页提供重新学习入口，并保持一次性XP和完成记录。
- [x] 清理第1～4课与当前精确审核音频策略冲突的历史“设备语音/音频未发布”可见文案。
- [x] Service Worker缓存升级为 `nikigo-v27-self-review-gate`，确保身份修复、永久门禁及用户可见文案更新不复用旧缓存。
- [x] 54条approved音频文件、catalog记录和完整SHA保持不变；音频API请求与费用为0。
- [ ] 等待产品负责人审核修复功能分支；未经确认不合并、不部署。
